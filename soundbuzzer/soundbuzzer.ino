int buzzerPin = 5;

void setup() 
{
  pinMode (buzzerPin, OUTPUT);
}

void loop() 
{
  
  for(int i = 0; i<200; i++)    //ank
  {
    digitalWrite(buzzerPin, HIGH);                  // c = 3830
    delayMicroseconds(2550/2);                      // d = 3400
    digitalWrite(buzzerPin, LOW);                   // e = 3038
    delayMicroseconds(2550/2);                      // f = 2864
    digitalWrite(buzzerPin, HIGH);                  // g = 2550
  }                                                 // a = 2272
                                                    // b = 2028
  for(int i = 0; i<200; i++)    //hon               // c = 1912
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, HIGH);
  }

  for(int i = 0; i<200; i++)  //mein
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0; i<200; i++)  //te       
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0; i<600; i++)    //ri
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0; i<300; i++)      //aja                
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, HIGH);
  }

  for(int i = 0; i<300; i++)    //b
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<200; i++)     //si
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2272/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2272/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<300; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3038/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3038/2);
    digitalWrite(buzzerPin, HIGH);
  for(int i = 0;i<300; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2272/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2272/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0; i<300; i++)      //aja                
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3400/2);
    digitalWrite(buzzerPin, HIGH);
  }

  for(int i = 0; i<300; i++)    //b
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<200; i++)     //si
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2272/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2272/2);
    digitalWrite(buzzerPin, HIGH);
  for(int i = 0;i<300; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2550/2);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2222);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2222);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(5432);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(5432);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(5432);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3465);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2333);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(5431);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2456);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(2456);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(5431);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(2546);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3415);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(3455);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(5212);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(5422);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(1345);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(5432);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(3453);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(1245);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(35);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(4);
    digitalWrite(buzzerPin, HIGH);
  }
  for(int i = 0;i<10000; i++)
  {
    digitalWrite(buzzerPin, HIGH);
    delayMicroseconds(989);
    digitalWrite(buzzerPin, LOW);
    delayMicroseconds(9000);
    digitalWrite(buzzerPin, HIGH);
  }
}
  }
}

